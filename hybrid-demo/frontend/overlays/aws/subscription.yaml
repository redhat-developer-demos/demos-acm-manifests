apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
   name: hybrid-demo-front-subscription
   labels:
      cloud: Amazon
spec:
   placement:
      placementRef:
         kind: PlacementRule
         name: frontend-dev-cluster-aws
